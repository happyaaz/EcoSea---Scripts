using UnityEngine;
using System.Collections;

public class Bubbles_MouseClick_Destroy : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Destroy (gameObject, 6.0f);
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
